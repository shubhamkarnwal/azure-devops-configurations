# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

resources:
 pipelines:
   - pipeline: shubhamkarnwal.azure-devops-test2
     source: shubhamkarnwal.azure-devops-test2
     project: shubhamkarnwal
     trigger: true

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo 'Pipeline details ${{ resources.pipeline.mySourcePipeline.pipelineID }}'
  displayName: 'Run a multi-line script'

